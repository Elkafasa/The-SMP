# This is the most important part. 
# 'kick' means if they aren't linked, they can't stay on the server.
  link-policy: kick

# This ensures that if they aren't linked, the bot ignores anything they try to do.
  block-unlinked-chat: true

# If they leave your Discord server, they are automatically unlinked 
# and will be kicked from the MC server next time they try to join.
  unlink-on-leave: true
role-sync:
  # If they lose a role on Discord, remove the corresponding group in MC.
  remove-roles: false
  remove-groups: true
  resync-delay: 5
  primary-group-only: false

  # NEW: Minecraft Group -> Discord Role sync.
  # This gives them the Discord role when they link and get the 'players' group.
  groups:
    "players": "1467584719838384270"

  # Discord Role -> Minecraft Group sync.
  # This ensures they have 'players' group in MC if they have the role in Discord.
  roles:
    "1467584719838384270": "players"
    